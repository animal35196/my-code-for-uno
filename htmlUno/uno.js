class tile{
	function drawCards{
		constructor(x,y,hight,width,text,textColor,textSlant){
			this.x=x;
			this.y=y;
			this.width=width;
			this.hight=hight;
			this.text=text;
			this.textColor=textColor;
			this.textSlant=textSlant;
		}
		userData
		var
	}
}.
socot.on(allClear){
	drawCards()
}